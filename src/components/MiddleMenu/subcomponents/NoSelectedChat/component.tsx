import React, { ReactNode } from 'react'
import { Wrapper } from './styles'

type Props = {
  children: ReactNode;
}

const NoSelectedChat = (props: Props) => <Wrapper>NoSelectedChat context</Wrapper>

export default NoSelectedChat
